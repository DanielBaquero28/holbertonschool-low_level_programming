#include "holberton.h"
#include <stdlib.h>

/**
 * print_diagsums - Adds the two diagonals of a square matrix of integers
 * @size: Pointer with size.
 * @a: Pointer type int.
 **/
void print_diagsums(int *a, int size)
{
int i, j, a;

i = 0;
while (s[i] != '\0')
{
a = 0;
for (j = 0; accept[j] != '\0'; j++)
{
if (s[i] == accept[j])
a = 1;
}
j = 0;
if (a == 1)
return (i + s);
i++;
}
return (NULL);
}
