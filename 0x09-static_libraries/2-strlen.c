#include "holberton.h"
#include <string.h>

/**
 * _strlen - Finds the length of a string
 * @s: The pointer from which the length of the string is found.
 *
 * Return: Nothing
 **/

int _strlen(char *s)
{

  int i;

  for (i = 0; s[i] != 0; i++)
    {

    }
  return (i);
}
