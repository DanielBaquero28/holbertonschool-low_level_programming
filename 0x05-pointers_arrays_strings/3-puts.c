#include "holberton.h"
#include <stdio.h>

/**
 * _puts - Updates a pointer to have the value of 98
 * @a: The pointer that swaps it's value with b
 * 
 * Return: Nothing
**/

void _puts(char *str)
{
puts(str);
}
